# Data-First Example

This example starts with plain JSON fixtures. jsondb infers collections, singleton documents, REST routes, GraphQL fields, and TypeScript types.

Run from the repository root:

```bash
node ./src/cli.js sync --cwd ./examples/data-first
node ./src/cli.js serve --cwd ./examples/data-first
```
