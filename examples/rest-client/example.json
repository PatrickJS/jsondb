{
  "title": "REST Client",
  "description": "A tiny consumer script that calls the local REST API with createJsonDbClient, including direct and batched requests.",
  "tags": ["client", "rest", "batching"]
}
